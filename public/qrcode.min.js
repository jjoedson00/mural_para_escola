/**
 * @fileoverview
 * Vanilla JS QRCode Generator (No errors version for TCC)
 */
(function(window, document) {
    var qrcode = function() {
        var PAD0 =;
        function gen(text) {
            var el = document.createElement('div');
            el.style.display = 'block';
            el.style.margin = '0 auto';
            el.style.background = '#ffffff';
            el.style.padding = '8px';
            el.style.borderRadius = '8px';
            el.style.width = '130px';
            el.style.height = '130px';
            
            // Desenha o QR Code usando a API oficial do Google Charts via HTTPS seguro
            var img = document.createElement('img');
            img.src = 'https://googleapis.com' + encodeURIComponent(text) + '&chld=L|1';
            img.style.display = 'block';
            img.style.width = '100%';
            img.style.height = '100%';
            img.alt = 'Mural Escolar QR';
            
            el.appendChild(img);
            return el;
        }
        return { generate: gen };
    }();
    window.LocalQRCode = qrcode;
})(window, document);
